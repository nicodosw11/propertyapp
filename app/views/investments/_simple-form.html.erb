<%= simple_form_for([deal, investment]) do |f| %>

  <%= f.input :price, input_html: { min: 1000 } %>
  <%= f.input :shares, input_html: { min: 1 } %>

  <%= f.button :submit, 'Invest', class: "btn-primary" %>
<% end %>
